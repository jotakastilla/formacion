// Main JavaScript file for the landing page

// Example: Track custom events with Vercel Analytics
// Uncomment the following code to track button clicks or other interactions

/*
document.addEventListener('DOMContentLoaded', function() {
  // Track custom events
  const buttons = document.querySelectorAll('button');
  
  buttons.forEach(button => {
    button.addEventListener('click', function() {
      if (window.va) {
        window.va('track', 'ButtonClick', {
          button: this.textContent
        });
      }
    });
  });
});
*/

console.log('Vercel Web Analytics is enabled on this site');
